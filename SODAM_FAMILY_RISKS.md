# SoDam 6형제 — 위험 등기부 (불편·빈틈·위험·오류·변수·충돌·실패)

> **2026-07-13 주석**: 원래 4형제 기준 문서. 본문은 2026-06-21 PRD 시점 분석이며, 최신 6형제 구현 실측·횡단 위험은 [[SODAM_FAMILY_STATUS]] 참조. §1·§3의 여러 **[가능성]이 실측으로 [확인됨] 승급**됨(ask 자동승인 무력·훅 중복 등).
>
> 작성: 2026-06-21 · 4개 플러그인 PRD를 **4팀 병렬 정밀분석** 후 종합.
> 정본 역할표 = SoDamAgentic §8 / 역할·경계 운영 = [[SODAM_FAMILY_MAP]] / **이 문서 = 위험·충돌 깊이 분석**.
> 태그: **[확인됨]**=PRD/조사에 직접 근거 · **[가능성]**=추론(스파이크·베타로만 확정).

---

## 0. 한 문장 결론

> **가장 큰 위험은 개별 플러그인이 아니라 "4개를 같이 깔 때"다.**
> 4팀이 **독립적으로 전부 1순위**로 같은 걸 지목했다 — **번들 동시설치 런타임 충돌**. 그런데 이 위험은 **4개 PRD 어디에도 없다**(전부 "혼자 깔 때"만 전제). 초보자 혼란의 최대 진원지가 여기다.

---

## 1. ★가족 공통 — 번들 동시설치 충돌 (최우선 · PRD 0건 · 대부분 [가능성])

| # | 충돌 | 같이 깔면 무슨 일 | 초보자 체감 | 심각도 | 소유규칙 해소책 |
|---|------|------------------|-------------|--------|----------------|
| 1 | **확인창 다중 발동** | Harness·Loop·Agentic이 각자 `PreToolUse` 훅 → 한 번의 `rm`에 확인창 2~4번 | "왜 같은 걸 네 번 물어?" → 안전벨트 신뢰 붕괴 | 🔴 | **안전 훅 = Harness 단독.** 형제는 훅 안 검, Harness 결정 재호출. 동시 발동 시 **더 엄격한 쪽(deny) 승 + 1회로 dedupe** |
| 2 | **백업 다중 + undo 분열** | Loop·Context·Agentic이 각자 백업 → `~/.sodamloop/` `~/.sodamcontext/` `~/.sodamharness/` 따로 | "되돌리기가 두 갠데 뭘 눌러?" → 엉뚱 복구(06이 막던 그 사고) | 🔴 | **백업·undo = Harness 단독 소유.** 형제는 "백업해줘"만 호출. 공유 `~/.sodam/backups/` |
| 3 | **말투 Skill 중복** | beginner-tone Skill을 형제마다 ship → 둘 다 활성 → 상충 어조 | AI가 명령마다 말투가 달라짐 | 🟠 | **말투 = 공유 1벌**(번들/Harness 소유), 형제 비간섭 |
| 4 | **권한모드 충돌** | Loop(자동반복)이 자동승인 모드 선호 ↔ Harness는 확인모드라야 `ask` 작동 | 한 형제가 동료의 안전벨트를 끊음(자동승인=ask 무력) | 🔴 | "권한모드 **하향(더 안전)만** 형제 가능, 상향은 Harness 거부권" |
| 5 | **Loop ↔ Harness 교착** | 자동반복 중 Harness 확인창이 뜨면 다음 회차 막힘 | "자동이라며 왜 계속 물어봐?" → '자동'이 '수동'으로 전락 | 🔴 | 루프 컨텍스트 **배치 승인**("1회 승인=이번 루프 N회 유효") |
| 6 | **Context 처방이 형제 규칙 삭제** | Context "줄이기"가 Harness/Loop이 심은 안전·반복 규칙을 군더더기로 오판·삭제 | 안전망이 **소리 없이** 사라짐(최악) | 🔴 | ① **안전·금지 키워드(`금지/never/must/항상/secret`) 자동 보존** ② 형제 주입 규칙 **보호 라벨**(처방 제외) ③ 삭제 대신 **스킬 이동** |
| 7 | **번들 컨텍스트 비대** | 4개가 각자 상시규칙·스킬설명을 얹어 → 사용자 한 글자 안 써도 컨텍스트 부풀음 | Context가 검진하면 "군더더기 많네요" = **가족이 자기 문제를 유발**하는 자가당착 | 🟠 | 형제는 상시로딩 최소·상세는 지연(Skill). Context는 "사용자분 vs 플러그인분" 구분 리포트 |

> **뿌리 = 공유코어(백업·undo·말투·안전)를 형제가 어떻게 호출하나가 어느 PRD에도 없음.** §8은 "단일 소유 + 위임"을 *정책*으로 정했지만, *런타임 계약*(누가 훅 소유, dedupe, 감지 실패 시 동작)은 비어 있다. → **§4 액션 1번**이 이걸 닫는다.

---

## 2. 플러그인별 치명 위험 TOP (압축 — 개별 PRD엔 대체로 인지됨)

**🛡️ Harness** (단독 설치 기준 가장 성숙)
- 🔴 **침묵하는 안전벨트** [가능성]: 훅이 안 돌면(Node없음/파싱크래시/매처범위밖) fail-**open** 위험 → "지켜준다 믿는데 무방비". → status에 **자가 헬스체크**(테스트 위험명령 넣어 deny 나오나).
- 🟠 **백업이 비밀파일 복사** [가능성]: `.env` 든 폴더 백업 시 §8.3(비밀 접근0) 자기위반. → 백업 **제외 필터**(`.env`,`*auth*`,`*.pem`).
- 🟠 **ask 피로 → 사용자가 안전벨트 끔** [가능성]: 정상작업마다 물으면 "전부 허용"으로 도망 → 무력화.

**🔁 Loop** (폭주·취소·세션 방어는 최강, 토대가 미검증)
- 🔴 **Skill 주도 반복 모터 자체가 미검증** [확인됨]: 외부 트리거 없이 스스로 N회 재진입하는 게 되는지 불명 → **되면 제품, 안 되면 공중**. fallback 부재.
- 🔴 **멈춤·되돌리기 진입점 미정의** [확인됨]: 약속한 3버튼(시작·멈춤·되돌리기) 중 2개의 실제 경로가 PRD에 없음.
- 🟠 **플랜 사용량 한도 전소** [가능성]: 30분 루프가 Pro/Max 5시간 한도를 소진 → 루프 도중 강제중단. $ 청구보다 자주 닥침. PRD가 **전혀 안 다룸**.

**🧭 Agentic** (동료 전환이 새 모순을 만듦)
- 🔴 **자기완결 ↔ 위임 톤이 한 문서서 충돌** [확인됨]: line133 "안전 훅 **반드시 내장**" vs §8 "안전 신규구현 **금지**·최소폴백만" → 개발 AI가 어느 쪽 따를지 무작위. **통일 1순위.**
- 🔴 **안전 위임 실패 = 무방비** [가능성]: Harness 미설치/구버전/감지실패 시 "최소폴백"만 남아 일반안전 통째 빔. → **fail-closed 승격**("Harness 확실할 때만 위임, 의심되면 폴백 전체로").
- 🟠 **Codex 안전 이중공백** [가능성]: 안전을 Harness에 위임했는데 **Harness는 Codex 미지원** → Codex 사용자는 폴백도 최소+Harness도 없음.

**📄 Context** (닭-알 + 처방 파괴)
- 🔴 **처방이 안전규칙 오판삭제** [가능성]: "줄이기"가 "force push 금지" 같은 꼭 필요한 줄을 빼면 겉은 건강·실제는 더 위험. → §1-6 보존규칙과 동일.
- 🟠 **닭-알 문제** [확인됨]: 핵심 사용자(파일 없는 초보자)는 검진을 못 씀 → 항상 문진으로 빠짐. 첫 카피를 "검진" 아닌 분기무관 표현으로.
- 🟡 **건강점수·6대문제 기준 미정** [확인됨/가능성]: 초보자 프로젝트엔 린터·git이 없어 Lint·Stale 탐지의 근거 데이터 자체가 없음. Phase1은 규칙 확실한 3종만 "확정", 나머지 "의심" 라벨.

---

## 3. 횡단 변수 (4개가 공통으로 깨지는 환경 — status가 감지·경고해야)

- **권한 모드**(기본 확인 vs acceptEdits/bypass) — 안전 **전체**를 좌우. 자동승인이면 ask 전부 무력. 4개 다 영향. [확인됨, Harness §8.8]
- **플랜 사용량 한도**(Pro/Max 5시간·주간) — Loop가 특히. PRD들이 $만 보고 rate limit 미고려. [가능성]
- **Windows 함정** — 한글/공백 홈경로(`C:\Users\홍길동`)·CRLF/LF·CP949·심링크 admin·junction/UNC. 형제 키트가 반복적으로 데인 곳. [확인됨, 메모리 전례]
- **도구 버전 변동** — 200줄/32KiB/훅 스키마/CVE 패치 버전(클코 2.0.65+)이 미래에 바뀌면 깨짐. [확인됨]
- **Codex 패리티** — 마켓 없음(수동설치)·훅 형식 다름·Harness Codex 미지원. "둘 다 지원" 성공기준의 실질 미달 위험. [확인됨]

---

## 4. 해소 액션 (가족 단위 · 빌드 전 · 우선순위)

1. **★번들 통합 명세 신설** — §8 역할표를 **런타임 계약**으로: 단일소유표(훅·백업·말투·안전 누가) + 위임 호출 방법 + **감지 실패 시 fail-closed** + 확인창 dedupe 규칙. (§1의 7개 충돌을 한 문서로 닫음)
2. **공유코어 1벌 먼저** — 백업·undo·말투·안전 게이트를 형제가 호출. 중복 원천 차단.
3. **현관 = Agentic 진입점**(§8) 이 라우팅 — 초보자가 4개 중 고르지 않게 "뭐 하고 싶어요?"로 안내.
4. **각 PRD에 "번들 공존" 섹션 추가** + 위 계약 링크. (특히 Agentic 톤 통일, Loop undo 주체, Context 보호라벨)
5. **빌드 순서**: 공유코어 → 🛡️Harness(이미 빌드완료, 토대) → 형제 1개 **베타 1명** → 확장. **4개 동시 빌드 금지**(§8 규칙4·반쯤 만든 SoDam 다수 = 헛심).

---

## 5. 정직 고지 (객관)

- **[가능성] 비중이 큼**: §1 번들 충돌 대부분은 PRD에 명세가 없어 **추론**이다. 실제 **동시설치 스파이크**로만 확정된다. 플러그인 코드·실동작·Codex 실거동은 **미검증**.
- **개별 PRD는 단독 설치 기준으론 성숙**(Harness 보안 ASVS·손시뮬, Loop 폭주방어, Agentic 정직한 한계, Context 자동삭제금지). 결함 대부분은 "PRD가 틀려서"가 아니라 **"번들"이라는 새 전제가 반영 안 된 것**.
- **내 앞선 "§8 부재" 단정은 오류였고 정정함**(Read 잘림 오판). 이 등기부도 추론을 포함하니 **같은 검증 원칙**을 적용 — "확인됨"만 사실로, "가능성"은 스파이크로 검증.
- 이 문서는 **문서일 뿐**. 실제 충돌 여부는 4개를 같이 깔아보는 통합 테스트로만 확인된다.
