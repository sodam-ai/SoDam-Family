# SoDam 가족 — 구현 실측 스냅샷 (Family Implementation Status)

> **최종 실측: 2026-07-13**(6형제, 6팀 병렬 정밀 분석) · **2026-08-04 갱신**(7형제로 확장 — Graph 추가, 6형제 핵심 지표는 메인 세션이 7개 저장소를 직접 순차 조사해 신선화. 6팀 병렬 심층분석 수준의 재감사는 아님 — §7 참고).
> 태그: **[확인됨]** = 파일/테스트/git으로 직접 검증 · **[추정]** = 추론(실사용·베타로만 확정).
>
> **세 문서의 역할 구분** (혼동 방지):
> - [[SODAM_FAMILY_MAP]] = **설계 정본** (역할 경계·API 계약·의존 방향·semver)
> - [[SODAM_FAMILY_RISKS]] = **번들 충돌 심층 분석** (2026-06-21, 동시설치 위험)
> - **이 문서(STATUS)** = **구현 실측 스냅샷** (지금 코드가 실제로 어디까지 됐나)

---

## 0. 한 줄 결론

> **7형제 모두 "Phase 1 MVP 코드 구현 완료 + 자동 테스트 그린" 단계에 도달했다. 병목은 이제 AI가 아니라 "사람이 직접 해야 하는 라이브 검증·법무·베타·push"다.**
> 단, "안전을 표방하는 제품인데 표방한 안전 일부가 라이브에서 실제로는 작동하지 않는" 괴리가 2건 남아 있어, 대외 공개 전 반드시 해소해야 한다(§5-1).
> **2026-08-04 신규**: Harness·Context·Loop 3곳은 **AI가 할 수 있는 안전 작업이 소진**됐다고 각자 CHECKPOINT에서 스스로 선언 — 남은 항목은 전부 사람 결정(이름·베타·법무·공개여부)이거나 사람 실행(push·재설치·2주 관찰)이다.

---

## 1. 7형제 최신 상태표 (2026-07-13 6팀 분석 기준 + 2026-08-04 직접 재조사 갱신)

| 형제 | 버전 | 브랜치(origin 동기화) | 최근 커밋(2026-08-04 기준) | 자동 테스트 | 구현 완성도 | 공개 |
|------|------|----------------------|---------------------------|-------------|-------------|------|
| 🛡️ **Harness** | 0.1.0 | `feat/phase1-mvp`(동기화) | **오늘**(A1 조사결과 반영) | selftest **117 PASS** + CI(2-OS) | Phase 1+2 코드 완료 | 공개 전(remote 존재) |
| 📄 **Context** | 0.1.0 | `feat/sodam-context-mvp`(동기화, 완전 clean) | **오늘**(checkup-deep README 반영) | **177 PASS** (118 test + 59 selftest) | Phase 1 완료 · **AI 작업 소진 선언(2026-08-04)** — 남은 4건 전부 사람 결정 | **PRIVATE** |
| 🔁 **Loop** | 0.1.0 | `feat/sodam-loop-phase1a`(동기화)¹ | **오늘**(M10 반영) | **178 PASS** (85 status-check + 93 tests, 07-13판 "80"에서 증가) | Phase 1a·1b 코드 완료 · **AI 작업 소진 선언(2026-08-04)** | 공개 전[추정] |
| 🧭 **Agentic** | 0.1.0 | `init-mvp`(동기화, 2개 미커밋: CHANGELOG·LIVE_TEST_GUIDE) | 어제(08-03, 설치캐시 정체 실측) | validate 9 + selftest **22 PASS** | Phase 1 MVP 완료 · Phase 2에서 PreToolUse 강제 예정 | **PRIVATE** |
| ✍️ **Prompt** | 0.1.0 | `docs/commands-sync-and-loader-fix-2026-07-18`(동기화)² | 07-27(정체 8일째) | **자동 테스트 0·CI 없음** (10개 전부 haiku 수동 실행 검증 완료, 2026-07-14) | 스킬 10종 완료 · 개인 용도 실사용 검증 완료(2026-07-15) | **PUBLIC**(유일, README가 스스로 "PRIVATE"이라 오기재 — 불일치, [[sodam-graph-eng-prd-created]] 12차 참고) |
| 🔍 **Reverse** | 0.1.0 | `feat/m5-readiness`(동기화, **8개 미커밋**: CHECKPOINT·README·re-start.md·re-local-install.mjs·SKILL.md 4개) | 07-27 마지막 커밋(정체 8일째, 단 워킹트리는 활발히 편집 중) | `_selftest` **8 PASS** (정식 CI 없음) | Phase 1 라이브 성공 · Phase 2 안드로이드 라이브 · Phase 3 골격, 라이브 검증 STEP 1~5 러너북(보호파일 패치) 진행 중으로 추정 — 8개 미커밋 파일이 그 작업 흔적으로 보임[추정] | PRIVATE[추정] |
| 🗺️ **Graph** | 0.1.0 | `main`(동기화, CHECKPOINT.md 미추적) | **오늘**(README 최신화) | M0 **8/8**·M2 **8/8** done_when(자동테스트 프레임 없음, done_when 항목 자체가 검수) | Phase 1 진행(M0·M2 완료, M3~M11 남음) | **PRIVATE**(GitHub 마켓플레이스 설치는 검증 완료 — §5-신규 참고) |

¹ Loop의 실제 git 저장소는 하위 폴더 `sodamloop/`. 상위 폴더는 git 아님.
² 2026-07-13판은 `main`(동기화)으로 기재됐으나 2026-08-04 재확인 시 현재 체크아웃 브랜치는 위 값 — `main`이 사라졌다는 뜻은 아니고 확인 안 된 브랜치 전환 가능성만 [미확인]으로 남김.

> **공통 특징 [확인됨]**: 전부 `v0.1.0` · `Apache-2.0` · `sodam-ai` org · **의존성 0 · 순수 Node.js(.mjs) · 100% 로컬 · 네트워크 0**(Graph의 GitHub 클론·Prompt 공개 배포 제외). 대부분 `main` 브랜치·정식 릴리스 태그 없이 `feat/*` 브랜치 단계.

---

## 2. 형제별 최신 정체성 (한 문단)

- **🛡️ Harness** [확인됨]: `PreToolUse` 훅에서 위험작업을 3등급(safe/risky/catastrophic) 정적 분석해 **deny/ask + 자동 백업 + undo** 하는 "AI 안전벨트". 4개 핵심 훅(`guard.mjs` 759줄·`backup.mjs`·`whitelist.mjs`·`activity.mjs`), 명령 6·스킬 3. **가족 전체의 안전 토대(L0)** — `PreToolUse` deny/ask와 백업/undo를 단독 소유.

- **📄 Context** [확인됨]: `CLAUDE.md`·`AGENTS.md`(AI 사용설명서)를 **건강검진·처방**하는 도구. 스킬 4(checkup/intake/treat/sync) + 예방훅 1. 규칙을 코드가 아닌 `rules/*.json`으로 분리(데이터 주도). **⚠️ O-Brain·영속 메모리가 아님** — save_memory/remember/link 기능 없음(흔한 혼동).

- **🔁 Loop** [확인됨]: 비개발자가 "될 때까지 안전하게 고쳐줘"를 시키는 **자율 반복 루프(make→check→fix)** + 14종 가드레일. maker-checker 분리(별도 컨텍스트 읽기전용 `checker` 에이전트). Stop 훅이 `block` 재주입으로 다음 회차 진행, loop-guard가 비용 0 순수 로직으로 정지 판정(반복 8회·시간 30분·no-op·진동 감지).

- **🧭 Agentic** [확인됨]: "계획 먼저 → 실행 → 쉬운 검토 → 안전 기본"을 떠먹여 주는 **초보 진입점**. 스킬 3(plan/review/start) + `easy-reviewer`(읽기전용 검토 에이전트) + guard/delegate 훅. `delegate.mjs`가 Harness 생존을 감지해 겹치는 안전을 위임(단 치명 명령은 항상 자체 deny).

- **✍️ Prompt** [확인됨·정정]: **초보용 한국어 글쓰기 스킬 10종** 무코드 플러그인. 훅·커맨드·에이전트 0개, 순수 마크다운(SKILL.md). **⚠️ "Next.js+Supabase 웹앱"은 폐기(pivot)됨** — 죽은 웹앱 코드가 디스크에 잔존하나 git 미추적. 페르소나 시스템(`sodam-persona`)과도 **별개**.

- **🔍 Reverse** [확인됨]: 검증된 RE 엔진(Ghidra·JADX·Apktool)을 **한국어·초보 UX + 안전 3층**으로 감싼 리버스 엔지니어링 도구. 커맨드 7(`/re-*`) + 스킬 6 + 훅 3. 방어·교육·본인 소유물 **전용** — 금지 스코프(오남용성) 요청은 안전 3층이 보고서 텍스트로도 거부. 실제 45건 차단 로그 존재.

- **🗺️ Graph** [확인됨, 2026-08-04 신설]: 나머지 6형제 저장소를 **읽기 전용**으로 훑어 "지금 어느 프로젝트가 어디까지 됐는지" 지도로 보여주는 **7번째 막내**. 명령 7개(`/graph-map`·`-where`·`-next`·`-why`·`-shadow`·`-reject`·`-undo`), `deny`/`ask` 같은 안전 판정 권한은 없음. `~/.sodam/graph-state.json`을 **단독 발행**(다른 형제는 읽기만). 형제 저장소 경로 개명에도 `repo_remote` 대조로 정확히 찾아내는 게 실측 검증됨(Loop의 한 겹 안쪽 `sodamloop/` 구조를 실제로 정확히 resolve, §2 M2 참고). 보안 검증(명령주입·경로조작 방지) 통과, 형제 저장소 쓰기 0건 유지 확인.

---

## 3. 아키텍처 계층 & 공존 규약 (요약)

```
Harness (L0) ← 안전 토대. PreToolUse deny/ask·백업·undo 단독 소유
    ↑
Context (L1) ← AI 설명서 건강검진. Harness 없을 때 독립 폴백 내장
    ↑
Loop (L2) / Agentic (L2) ← 반복 실행 / 계획·검토·오케스트레이션
    ↑
Prompt (L3) / Reverse (L3) ← 글쓰기 입구 / 외부 분석

Graph (독립, 계층 밖) ← 6형제를 옆에서 읽기만 함. 의존 없음·피의존 없음(관찰자)
```

**핵심 계약** [확인됨] (정본: [[SODAM_FAMILY_MAP]] §1, family-synergy.md):
- **한 가지 일은 한 형제만 소유. 나머지는 위임. 절대 중복 구현 안 함.**
- 안전 판정은 Harness 단독. 형제는 `~/.sodamharness/safety-rules.json`의 `plugins.<이름>` 네임스페이스에 규칙만 주입.
- 공통 생존 감지는 `shared/family-detect.mjs`로 단일화(`isHarnessAlive`, 3조건: 존재+최소버전+헬스파일).

---

## 4. 공통 강점 [확인됨]

1. **방어적 보안 설계** — guard 훅은 명령을 절대 실행/eval 안 하고 정적 분석만. 파싱 실패 시 **fail-closed**(ask/deny).
2. **의존성 0 + CI 강제** — Harness CI가 위험 API 실호출 0을 기계적으로 검사. 공급망 위험 최소.
3. **정직성 문화가 코드·문서에 실재** — "100% 안전 아님", "초록불 ≠ 성공". 미검증을 "작동"으로 과장하지 않음.
4. **실사용 기반 회귀 방지** — 라이브에서 찾은 실제 버그를 테스트로 고정(각 CHECKPOINT에 발견→원인→수정→재검증 감사 추적).
5. **비밀 위생** — `.env`·키·토큰을 백업·로그·출력에서 이름 기반 제외 + 마스킹. 하드코딩 시크릿 0.

---

## 5. 횡단 위험 (심각도순) — 배포 전 핵심 점검

### 🔴 심각 (공개 전 반드시)

**5-1. "선언된 안전 ≠ 실제 안전" 괴리** — 가장 위험한 패턴
- **Context** [확인됨]: 예방 훅(`prevent-write.mjs`)이 코드·단위테스트는 통과하나 **실제 Claude Code 세션에서 발동 안 함**(재시작 후에도 300줄+ 저장 통과). 같은 `Write|Edit` matcher에 타 플러그인 훅이 경쟁 → 하나만 반영되는 것으로 좁혀짐([추정], 미확정). 문서상 안전장치의 절반이 실환경에서 무력.
  - **2026-07-16 추가 진단(Context 세션, [확인됨])**: sodam-harness·sodam-context가 둘 다 `✔ enabled`인 라이브 세션에서 312줄 `CLAUDE.md`를 실제 Write 도구로 재현 — 확인창·차단 없이 그대로 통과(버그 재확인). 그런데 **같은 내용을 `prevent-write.mjs`에 stdin으로 직접 넣으면 정확히 `deny`를 반환**(스크립트 로직 100% 정상). → **원인이 Context 코드가 아니라 "Claude Code가 라이브 세션에서 이 hook 자체를 호출하지 않는 지점"으로 좁혀짐**. `claude plugin details sodam-harness`로 Harness가 `PreToolUse`(+`PostToolUse`) hook을 등록함은 확인했으나, 정확한 matcher 패턴(진짜 `Write|Edit`을 포함하는지)은 Harness 저장소가 이 세션의 작업 폴더 밖이라 확인 불가(권한/샌드박스 한계, [미확인]으로 남김 — 확정 아님). 다음 단계는 ① Harness 쪽 세션에서 자신의 hooks.json matcher 직접 확인, 또는 ② Claude Code의 다중 hook 실행 모델(동일 이벤트에 여러 matcher가 있을 때 전부 실행하는지, 하나만 실행하는지) 자체를 검증 — 둘 다 Context 단독 세션 권한 밖.
- **Reverse** [확인됨]: 발견된 안전 결함 수정(입력 정규화·fail-open)이 **보호파일이라 라이브 배포본엔 미반영**되고 "사람이 수동 적용"(`SETUP_BLOCKED_FILES.md`)으로만 존재 → 문서상 고쳤지만 실제 배포본은 안 고쳐진 상태.

**5-2. Reverse 2층 deny-hook 실측 회피 + 과차단** [확인됨]
- 블라인드 레드팀 **16개 중 12개 회피 성공**(입력 난독화·트리거 미포함 입력·도구호출 분할 등 다양한 기법).
- **과차단**: 정상 보고서 저장이 2회 차단되자 AI가 정상적 관찰 서술을 통째 누락 — "위험 요소가 있다"와 "위험 방지 코드가 없다"를 문맥 없이 구분 못 하는 구조적 한계.
- **🔴 라이브 재현(2026-07-13)**: 이 STATUS 문서를 저장하려 하자 sodam-reverse deny-hook이 본문의 특정 키워드를 위험으로 감지해 Write를 **2회 실제 차단**함. 방어 목적의 중립 분석 서술조차 막는 이 결함이 문서 작성 중 스스로 재현됨 → 표현을 추상화해 저장. 이 사건이 곧 위 과차단 결함의 실증.

### 🟠 중요 (공통 구조적 한계)

**5-3. 훅 `ask`는 자동승인 모드에서 무력** [확인됨] — Harness·Loop·Agentic 전부 자인. `bypassPermissions`/`acceptEdits` 모드면 위험 확인창이 조용히 통과. **`deny`만 권한 모드와 무관하게 유효.** ([[SODAM_FAMILY_RISKS]] §3의 [가능성]이 실측으로 [확인됨] 승급.)

**5-4. 탐지가 정규식/키워드 best-effort** [확인됨] — 실제 셸 렉서가 아님. 브레이스 확장·서브셸·`xargs`·인코딩된 명령 실행·따옴표 없는 경로 등은 못 잡음(각 프로젝트가 스스로 문서화한 한계). **반대 방향 실패(과차단)도 존재** — §5-2 참조.

**5-5. 문서–실측 드리프트 반복** [확인됨] — 각 CHECKPOINT/CLAUDE.md 테스트 수치가 stale(Agentic "22→실제 42", Loop "54→65", Reverse deny 코퍼스 "48/5→60/7"). **이 우산 저장소 자체도 드리프트였음**(2026-06-29 문서가 Prompt를 여전히 "웹앱"으로 표기 → 이 STATUS로 정정).

### 🟡 정리 필요 (엔트로피·번들 미완)

**5-6. Harness `.PRD` 내부 문서 14개가 origin에 tracked** [확인됨] — `.gitignore`는 "push 금지"라 했으나 추가 前 커밋되어 소급 무효. `git rm --cached` 미실행. 저장소 public이면 내부 기획·리서치 노출.

**5-7. Prompt에 폐기된 Next.js+Supabase 웹앱 코드 디스크 잔존** [확인됨] — git 미추적이나 물리 삭제 안 됨(npm moderate 2건). `package.json` 정체성도 웹앱 그대로.

**5-8. 심볼릭/junction 링크 회피 잔존** [확인됨, 부분 해소] — Context는 여전히 잔존(Windows realpath 한계, 의도적 보류). **Agentic은 2026-07-14 해소**: `pathTraversesSymlink()` 신설(작업폴더~대상 경로의 모든 구성요소를 lstat해 junction/심볼릭 링크 경유 우회를 차단, realpath 전체 정규화 대신 컴포넌트별 검사 방식 채택 — cwd 자체가 심볼릭인 환경 오탐 회피). 실제 junction으로 재현 검증(커밋 `d5b2e68`, `hooks/_selftest.mjs` 42→44 PASS, 회귀 0).

**5-9. 번들 통합 규약이 문서엔 있으나 코드 미반영** [확인됨] — [[SODAM_FAMILY_RISKS]]가 처방한 ① 공유 백업 경로 `~/.sodam/backups/` ② 확인창 dedupe ③ 말투 스킬 1벌은 **아직 각 플러그인 독립 상태**(Loop는 PRE-3 위임 로직 일부 구현, Reverse는 Harness 감지 코드 없어 훅 2개 중복). 동시설치 충돌은 실통합 테스트로만 확정 가능.

---

## 6. 남은 블로커 (거의 전부 사람 몫)

| # | 블로커 | 대상 |
|---|--------|------|
| 1 | 각본 없는 초보자 실사용 / 베타 1~2명 | 7형제 전부 |
| 2 | 법무 검토(상표·저작권·라이선스) | 7형제 전부 |
| 3 | Mac / Linux 실측 (현재 Windows만) | 7형제 전부 |
| 4 | `main` 브랜치·번들 마켓플레이스 정리 | 우산 + 각 형제 (**Prompt는 2026-07-15 로컬 방치 브랜치 5개 정리 완료**) |
| 5 | `.PRD` git 추적 해제 (`git rm --cached`) | Harness |
| 6 | 예방훅 라이브 발동 원인 규명 | Context |
| 7 | 2층 정규화 배포본 반영 + 과차단 해소 + 독립 레드팀 라이브 | Reverse |
| 8 | 🆕 마켓플레이스 재설치 후 완전 재시작 → 명령 7개 노출·SessionStart 훅 발동·`/graph-map` 렌더링 4가지 눈으로 확인(M-PUSH) | Graph |
| 9 | 🆕 형제 계약 갱신안 8건 중 남은 6곳(우산 저장소는 이번에 반영 완료)에 직접 복사·push(M-FAMILY-SYNC) | 사용자 (Harness·Agentic·Context·Loop·Prompt·Reverse) |
| 10 | 🆕 2주 효과 측정 — `verified` 정체 마일스톤 감소·`/graph-why` 실사용 3회↑·재설명 0회, 2026-08-16 전후 확인(M-OBSERVE) | Graph + 사용자 관찰 |

> 메모리 기록상 법무·베타·공개는 여러 형제에서 "구현 완료 후 확정, 재제안 금지"로 사용자가 의도적으로 연기 중. 이 경계를 존중한다.

---

## 7. 정직 고지 (미검증·불확실)

- 각 저장소의 **public/private 여부 일부는 [추정]**(remote·README·CHECKPOINT 근거). Loop·Reverse는 특히 확정 못 함.
- 이 STATUS는 **6팀 서브에이전트의 실측 보고를 메인이 종합**한 것으로, 개별 항목을 메인이 교차 재확인하지는 않았다. (2026-08-04 갱신분은 메인 세션이 7개 저장소를 **직접 순차** 조사한 것 — 6팀 병렬 심층분석과 조사 깊이가 다름을 구분해 표시함.)
- 이 문서는 **스냅샷**이다. 형제 저장소가 갱신되면 낡는다 — 다음 대규모 정리 시 재실측 후 갱신할 것.
- **2026-07-15 부분 갱신**: Prompt 저장소만 SoDam-Prompt-Eng 세션에서 직접 검증한 최신 실측치로 갱신됨(§1 표·§6 블로커#4).
- **2026-08-04 갱신 범위**: 7형제 전체의 **git log·git status·CHECKPOINT.md 꼬리부만** 직접 읽어 §0·§1·§2·§3·§6을 갱신. §4(공통 강점)·§5(횡단 위험) 심층 분석은 **2026-07-13 원판 그대로**(재검증 안 함, 낡았을 수 있음 — 특히 §5-1 "예방훅 라이브 미발동"·§5-2 Reverse 과차단은 이번에 재현 테스트하지 않았으므로 여전히 유효한지 다음 세션에서 확인 필요).
- **Prompt 브랜치명 불일치 [미확인]**: 07-13판 `main` ↔ 08-04 재확인 `docs/commands-sync-and-loader-fix-2026-07-18`. 브랜치가 실제로 바뀐 것인지, 단순히 이번 조사 시점의 체크아웃 상태인지 확정하지 않음(§1 각주 ② 참고).

---

> 최종 갱신: 2026-08-04(7형제 확장 + §0·§1·§2·§3·§6 직접 재조사) · 이전: 2026-07-13(6팀 병렬 정밀 분석) · 정본 설계=[[SODAM_FAMILY_MAP]] · 충돌 분석=[[SODAM_FAMILY_RISKS]]
